from launch import LaunchDescription


def generate_launch_description():
    # This launch file was converted from the legacy XML which contained no nodes.
    # Keep an empty launch description so includes remain valid.
    return LaunchDescription()
